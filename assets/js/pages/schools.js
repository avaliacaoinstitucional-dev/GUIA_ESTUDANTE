
import { GUIDE_DATA } from '../data/courses.js';
import { layout } from '../components/layout.js';
const html=GUIDE_DATA.schools.map(s=>{const courses=GUIDE_DATA.courses.filter(c=>c.schools.includes(s)); return `<article class="card school-card"><h3>${s}</h3><p>${courses.length} curso(s) mapeado(s)</p><div class="tagrow">${courses.slice(0,10).map(c=>`<a class="tag" href="curso.html?id=${c.id}">${c.title.replace('Tecnologia em ','')}</a>`).join('')}${courses.length>10?`<span class="tag">+${courses.length-10}</span>`:''}</div><div class="hero-actions"><a class="btn btn-ghost" href="cursos.html?school=${encodeURIComponent(s)}">Ver todos da unidade</a></div></article>`}).join('');
layout(`<section class="container section"><span class="eyebrow"><i class="ri-map-pin-line"></i> escolha pela localidade</span><h1 class="big-title">Cursos por unidade</h1><p class="lead">Veja rapidamente quais cursos aparecem em cada localidade mapeada na planilha.</p><div class="schools-grid">${html}</div></section>`, 'escolas');
